stages:
  - build

addReadme:
  stage: build
  script:
    - echo "This is a README file" > README.md
    - git config user.email "you@example.com"
    - git config user.name "Your Name" 
    - bin/gitlab-commit-and-push "Add README.md"
